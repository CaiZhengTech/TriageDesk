# Secrets — never commit
.env
.env.*
!.env.example
*.pem
credentials*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.vercel/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local data / scratch
*.sqlite3
.ipynb_checkpoints/
data/
# ...except triagedesk/data/ — committed generated code artifacts (e.g. queue_centroids.json)
!triagedesk/data/
!triagedesk/data/**

# Task 6: judge-calibration label export -- contains ticket text, never commit
judge_labels*.csv

# superpowers local workspace: the raw ledger, brainstorm session state
# (contains a local auth token), and regenerated review diffs. Per
# CLAUDE.md these stay local — never committed.
.superpowers/
